import React, { PropTypes } from 'react';

const admin = (props) => {
    return (
        <div>
        	<h1> ddaay laf trang admin</h1>
        </div>
    );
};

admin.displayName = 'admin';

admin.propTypes = {
};

export default admin;
